{
  "author": "Clement Hoang",
  "name": "reapp-squad",
  "repository": "",
  "description": "",
  "version": "0.0.1",
  "main": "./app/app.js",
  "reapp": "0.8",
  "scripts": {
    "test": "",
    "start": "reapp run",
    "prepare-android": "rm -rf ../www && cp -a build/android/. ../www/",
    "prepare-ios": "rm -rf ../www && cp -a build/ios/. ../www/"
  },
  "dependencies": {
    "react": "0.13.2",
    "reapp-kit": "^1.2.0",
    "reapp-pack": "^1.3.26"
  }
}
